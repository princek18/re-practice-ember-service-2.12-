
import { currencyConverter } from 're-practice/helpers/currency-converter';
import { module, test } from 'qunit';

module('Unit | Helper | currency converter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currencyConverter([42]);
  assert.ok(result);
});

